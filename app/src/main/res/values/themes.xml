<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TodoAppXml" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    
    <style name="SplashScreenTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/themeBackground</item>
        <item name="colorPrimaryDark">@color/themeBackground</item>
        <item name="colorAccent">@color/themeBackground</item>
    </style>

    <style name="Theme.TodoAppXml" parent="Base.Theme.TodoAppXml" />
</resources>